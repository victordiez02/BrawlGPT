# Core API
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0,<0.35.0
pydantic>=2.9.0,<3.0.0
python-dotenv>=1.0.1

# Google Gemini SDK (new official client)
google-genai>=0.8.0

# HTTP client (for retries / external calls)
httpx>=0.27.0

# Console pretty-printing (only used by CLI scripts)
rich>=13.9.0
termcolor>=2.5.0
tabulate>=0.9.0
